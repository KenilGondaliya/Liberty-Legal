"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.flattenEmailData = void 0;
/**
 * Converts given send email data object to Mailtrap format.
 */
const flattenEmailData = (params) => {
    const { sender, recipient, subject, text, html, attachments } = params;
    return {
        to: [
            {
                email: recipient.email,
                name: recipient.name
            }
        ],
        from: {
            email: sender.email,
            name: sender.name
        },
        subject,
        ...text && { text },
        ...attachments && { attachments },
        ...html && { html }
    };
};
exports.flattenEmailData = flattenEmailData;
